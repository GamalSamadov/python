"""
--------------------------------------------------
---------------------- Pip -----------------------
--------------------------------------------------

pip ==> Package Installer for Python 
pip intalls automatically on python 3.4 and leater and python 2.7.2 and leater

get site of packages:
import site
site.getsitepackages()

--------------------------------------------------
Commands:

Create a new folder for the project:
mkdir {directory_name}

Go to the project folder:
cd {directory_name}

Create a virtual environment:
python -m venv {env_name}

Installing the venv package in Ubuntu:
python3 -venv

Installing the venv package in Debian and its variants:
sudo apt install python3 -venv

Browse folders:
dir

Activate Virtual Environment on Linux or Mac:
source venv/bin/activate

Activate the virtual environment on Windows:
venv/scripts/activates

Browse the list of commands for the pip package manager:
pip

Browse the list of pip package manager commands on Linux or Mac:
pip3

Browse the current version of the package manager:
pip –version

Installing the Flask framework:
pip install flask

Browse installed packages:
pip list

To see specific package information:
pip show {package_name}

Create a file containing a list of package names and versions:
pip freeze > {file_name.txt}

Update the versions of the libraries installed in the project:
pip install –upgrade -r {file_name.txt}

Installing packages for the project:
pip install -r {file_name.txt}

Uninstalling a specific package:
pip uninstall {package_name}

Uninstall all packages specified within a file:
pip uninstall -r {file_name.txt}


list of Packages is on:
pypi.org


"""


